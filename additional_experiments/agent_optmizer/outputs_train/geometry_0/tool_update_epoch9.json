[
  {
    "action": "add",
    "target": {
      "code": "def find_prime_pairs(k):\n    m = k\n    n = 1\n    return m, n",
      "description": "Find relatively prime pairs (m, n) from an integer k represented as k = m/n.",
      "name": "find_prime_pairs",
      "packages": ""
    }
  },
  {
    "action": "add",
    "target": {
      "code": "def calculate_triangle_area(base, height):\n    return (base * height) / 2",
      "description": "Calculate the area of a triangle given its base and height.",
      "name": "calculate_triangle_area",
      "packages": ""
    }
  },
  {
    "action": "add",
    "target": {
      "code": "from sympy import pi\n\ndef calculate_area(shape_type, dimensions):\n    if shape_type == 'circle':\n        radius = dimensions['radius']\n        return pi * radius ** 2\n    elif shape_type == 'triangle':\n        base = dimensions['base']\n        height = dimensions['height']\n        return (1/2) * base * height\n    else:\n        return None",
      "description": "Calculate the area of a shape (circle, triangle, etc.) based on shape type and its dimensions.",
      "name": "calculate_area",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "def calculate_surface_area_of_cube(edge_length):\n    return 6 * edge_length ** 2",
      "description": "Calculates the surface area of a cube given the length of an edge.",
      "name": "calculate_surface_area_of_cube",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "from sympy import pi\n\ndef calculate_volume_of_cone(radius, height):\n    return (1/3) * pi * radius ** 2 * height",
      "description": "Calculate the volume of a cone given its radius and height.",
      "name": "calculate_volume_of_cone",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "from sympy import pi\n\ndef calculate_cylinder_volume(radius, height):\n    return pi * radius ** 2 * height",
      "description": "Calculate the volume of a right circular cylinder given its radius and height.",
      "name": "calculate_cylinder_volume",
      "packages": "sympy"
    }
  }
]