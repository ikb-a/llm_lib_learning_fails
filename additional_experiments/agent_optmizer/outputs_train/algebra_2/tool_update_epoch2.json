[
  {
    "action": "add",
    "target": {
      "code": "from sympy import sqrt\n\ndef calculate_distance(x1, y1, x2, y2):\n    dx = x2 - x1\n    dy = y2 - y1\n    distance = sqrt(dx**2 + dy**2)\n    return distance",
      "description": "Calculate the distance between two points in a coordinate system.",
      "name": "calculate_distance",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "def calculate_total_ants(initial_species_a, initial_species_b, days, growth_a, growth_b):\n    final_species_a = initial_species_a * (growth_a ** days)\n    final_species_b = initial_species_b * (growth_b ** days)\n    total = final_species_a + final_species_b\n    return total",
      "description": "Calculate the total number of ants for two species based on given growth rates and initial conditions.",
      "name": "calculate_total_ants",
      "packages": ""
    }
  },
  {
    "action": "add",
    "target": {
      "code": "from sympy import symbols, Eq, solve\n\ndef solve_linear_equation(a, b):\n    x = symbols('x')\n    equation = Eq(a * x + b, 0)  # Assuming ax + b = 0\n    return solve(equation, x)[0]",
      "description": "Solve a linear equation given in standard form.",
      "name": "solve_linear_equation",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "from sympy import sympify, SympifyError\n\ndef evaluate_expression(expression):\n    try:\n        result = sympify(expression)\n        if isinstance(result, (list, tuple)):\n            return [float(r) if r.is_number else str(r) for r in result]\n        return float(result) if result.is_number else str(result)\n    except SympifyError as e:\n        return str(e)",
      "description": "Evaluate arithmetic or mathematical expressions provided as strings.",
      "name": "evaluate_expression",
      "packages": "sympy"
    }
  },
  {
    "action": "add",
    "target": {
      "code": "def calculate_total_cost(price_per_pound_corn, price_per_pound_beans, pounds_of_corn, pounds_of_beans):\n    return (price_per_pound_corn * pounds_of_corn) + (price_per_pound_beans * pounds_of_beans)",
      "description": "Calculate the total cost given the price per item and quantities.",
      "name": "calculate_total_cost",
      "packages": "sympy"
    }
  }
]